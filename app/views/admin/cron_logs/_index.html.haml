%table#cron_log.table.table-bordered.table-hover.table-condensed
  %thead
    %tr
      %th
        Log
        &mdash;
        = link_to 'Longer Log', admin_cron_logs_path
      %th Occurred At

  %tbody
    - @cron_logs.each do |cron_log|
      %tr
        %td
          .cron-details
            = cron_log.log
            = link_to_function('details', '', class: 'show-hide-details') if cron_log.details.present?
          .cron-log-details.hide= simple_format cron_log.details
        %td= cron_log.created_at.utc

