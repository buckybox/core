.row
  %table.twelve.columns#transactions
    %thead
      %tr
        %th Date
        %th Details
        %th Transaction
        %th Balance

    %tbody
      - transaction_total = Money.new(0) # manual calculation until we start storing historical balance

      - @transactions.each do |transaction|
        %tr
          %td= transaction.display_time.to_s(:transaction)
          %td= transaction.description
          %td= transaction.amount.format
          %td= (@balance - transaction_total).format

        - transaction_total += transaction.amount

