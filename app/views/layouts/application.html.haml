- content_for :body do
  .container
    .row
      .span12
        - flash.each do |kind, message|
          = flash_bar(message, kind: kind)
    .row
      .span12
        - if show_title?
          %h1= yield(:title)

        = yield

= render 'layouts/base'
