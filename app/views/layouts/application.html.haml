- content_for :body do
  .container
    .row
      .twelve.columns
        - flash.each do |kind, message|
          = flash_bar(kind, message)
    .row
      .twelve.columns
        - if show_title?
          %h1= yield(:title)

        = yield

= render 'layouts/base'
