- stylesheet 'customer'
- javascript 'customer'

- content_for :body do
  = render partial: 'layouts/customer/banner', locals: { distributor: @distributor }

  .container
    = render_site_messages(flash)

    .row
      #content-information.span3
        #information= yield(:sidebar)
      #content-body.span9
        - if show_title?
          .row-fluid
            .span12.text-center
              %h2= yield(:title)

        .row-fluid
          .span12= yield

= render 'layouts/base'
