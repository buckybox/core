= render 'layouts/head'

%body
  .container
    .row
      .twelve.columns
        - flash.each do |name, msg|
          = content_tag :div, msg, :id => "flash_#{name}"
 
        - if show_title?
          %h1= yield(:title)
 
        = yield
