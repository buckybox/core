.row
  %table.twelve.columns
    %thead
      %tr
        %th Number
        %th Customer
        %th Amount
        %th Date
        %th Paid

    - invoices.each do |invoice|
      %tbody
        %tr
          %td= invoice.id.present? ? link_to("#{invoice.full_number}", [current_distributor, invoice]) : '-'
          %td= link_to invoice.account.customer.name, [current_distributor, invoice.account.customer]
          %td= invoice.amount
          %td= invoice.date
          %td= checkmark_boolean(invoice.paid)

