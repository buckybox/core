= simple_form_for([:distributor, @customer], html: { class: 'form-horizontal' }) do |f|
  = f.error_notification

  .row-fluid
    .span12
      = render partial: 'delivery_form_inputs', locals: { f: f }

  .row-fluid
    .span12.form-actions
      = f.button :submit, 'Update'
      or
      = link_to 'cancel', :back
