- title("Add to #{@account.customer.name}'s Balance")

.row
  .columns.four= render 'form'

= link_to 'Show', [current_distributor, @account]
\|
= link_to 'Back', distributor_accounts_path(current_distributor)
