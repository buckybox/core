$("tr.row-fluid_details:last").after('<%= j(render partial: "distributor/import_transactions/rows") %>');
window.payments.reload();
<% if @last_row_reached %>
  $("tr.row-fluid_action:last a").hide();
<% end %>
