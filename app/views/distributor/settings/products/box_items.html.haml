- content_for :product_type do
  .row-fluid
    .span6
      %strong Box Items
      are the items that may go into your lines.
      During the checkout process, customers may nominate to exclude and substitute items in this list.
    .span6
      .label Hint:
      Make sure your
      = link_to "Boxes", distributor_settings_products_box_items_path
      are configured to allow excludes and substitutes for this list to take effect.

    -# %tr
    -#   %td{colspan: 4}
    -#     - first_line_item = line_items.one?
    -#     = link_to 'javascript:void(0)', class: (first_line_item ? 'required-action' : 'action'), 'data-toggle' => 'collapse', 'data-target' => '#new_line_item' do
    -#       Add
    -#       = first_line_item ? '' : 'or edit'
    -#       <strong>Box Items</strong>

    -#         %td
    -#           %strong= link_to line_item.name, 'javascript:void(0)', 'data-toggle' => 'collapse', 'data-target' => "#edit_line_item_#{line_item.id}"
    -#         %td.text-right
    -#           STUFF

  = simple_form_for(line_items, url: distributor_settings_products_box_items_path, html: { novalidate: false, class: 'background-yellow' }) do |f|
    %table.table.table-condensed.spacer
      - line_items.each do |line_item|
        %tr.edit
          %td
            = f.input_field :name, class: 'name', required: true, placeholder: 'Name'
          %td.text-right
            STUFF

    .form-actions
      = link_to 'cancel', 'javascript:void(0)'
      = f.button :submit, 'Save', class: 'btn btn-primary'

= render partial: 'distributor/settings/products', locals: { type: 'box_items' }

