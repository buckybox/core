%p= t('.new_details', distributor: @distributor.name)

%p= new_customer_session_url(distributor: @distributor.parameter_name)

%p<
  = t('.email') << t('colon') << @customer.email
  %br
  = t('.password') << t('colon') << @customer.password

%p
  = t('.instructions_html')
